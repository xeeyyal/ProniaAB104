@model List<Category>
@{
    int count = 0;
}
<div class="main-panel">
    <div class="content-wrapper">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Category Table</h4>
                    <a asp-action="create" asp-controller="category" class="btn btn-success ">Create Category</a>
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>No</th>
                                    <th> Name</th>
                                    <th> Products Count</th>
                                    <th> Settings</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (Category category in Model)
                                {
                                    count++;
                                    <tr>
                                        <td> @count</td>
                                        <td> @category.Name </td>
                                        <td> @category.Products.Count </td>
                                        <td>
                                            <a class="btn btn-info "> <i class="mdi mdi-pen"></i> Update</a>
                                            <a class="btn btn-danger  "> <i class="mdi mdi-delete"></i> Delete</a>
                                            <a class="btn btn-secondary "> <i class="mdi mdi-alert-box"></i> Details</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>